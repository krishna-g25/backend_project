import { Router } from 'express';

const router = Router();

// Add project routes here
// Example: router.route('/create').post(createProject);
// Example: router.route('/list').get(getProjects);
// Example: router.route('/:id').get(getProjectById);

export default router;
